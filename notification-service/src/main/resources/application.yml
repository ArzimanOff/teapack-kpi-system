server:
  port: 8086

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:postgresql://localhost:5432/teapack_kpi_db
    username: teapack
    password: teapack123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        default_schema: notification

  flyway:
    enabled: true
    schemas: notification
    locations: classpath:db/migration
    baseline-on-migrate: false
    default-schema: notification
    validate-on-migrate: false

# Пороговые значения KPI больше не задаются здесь —
# хранятся в notification.kpi_thresholds (см. V2-миграцию),
# меняются через PUT /api/thresholds (роли TECHNOLOGIST/ADMIN).

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

logging:
  level:
    com.teapack.notification: DEBUG